{
  "name": "@dtpt/jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./*": {
      "types": "./src/*.ts",
      "default": "./dist/*.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start:notify": "node dist/notify/cli.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "pnpm --filter @dtpt/core build && pnpm -w exec vitest run packages/jobs/src"
  },
  "dependencies": {
    "@dtpt/core": "workspace:*",
    "@effect/platform-node": "4.0.0-beta.65",
    "effect": "4.0.0-beta.65"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-beta.65"
  }
}
